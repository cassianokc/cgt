Compiling:
	To compile the code simply use:
	$make all
	This command will generate an executable cgt.

Calling: 
	To call the program use ./cgt, you can then put the source that you want to make the lexical analysys, there are one example that you can run using:	$./cgt < example.in
